export default function Interviews() {
  return (
    <div className="rounded-2xl border border-dashed border-indigo-200 bg-indigo-50/40 p-8 text-sm text-indigo-600">
      Interview progress board (hook to /api/interviews + /api/applications). Visualize status, steps, outcomes, and dates.
    </div>
  );
}
